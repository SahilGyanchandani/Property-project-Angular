/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PropertyCitySearchPipePipe } from './PropertyCitySearchpipe';

describe('Pipe: PropertyCitySearchPipee', () => {
  it('create an instance', () => {
    let pipe = new PropertyCitySearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});

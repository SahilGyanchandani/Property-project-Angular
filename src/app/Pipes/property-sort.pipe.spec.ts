import { PropertySortPipe } from './property-sort.pipe';

describe('PropertySortPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertySortPipe();
    expect(pipe).toBeTruthy();
  });
});
